import 'package:flutter/material.dart';

/// The one brand color that stays constant across Light/Dark/System — the
/// "glow" accent from the Lumina mark (see LuminaMark). Everything else
/// about the mark adapts to the surrounding theme; this doesn't, by design
/// (approved brand pitch: "the glow color stays constant, the ink flips").
const luminaGlow = Color(0xFFF4B740);
